/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains:['images.unsplash.com','res.cloudinary.com']
    }
};
//to configure images of unsplash we need to add domains in this files
export default nextConfig;
